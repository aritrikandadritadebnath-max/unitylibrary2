<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Unity Library | E-Corner Platform</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,600;0,800;1,600&family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
        /* --- ADVANCED CSS VARIABLES (BRONZE + GREEN AMBIENT + IMAGE DESIGN) --- */
        :root {
            --bg-cream: #fdf8f4;
            --bg-header-curve: #eae1d5;
            --bronze-dark: #8c6a46;
            --bronze-mid: #bfa58a;
            --bronze-light: #d4a373;
            --bronze-gradient: linear-gradient(135deg, #d4a373, #8c6a46);
            --green-dark: #1b4332; 
            --green-light: #d8f3dc;
            --text-main: #2c241d;
            --text-muted: #7f7a75;
            --white: #ffffff;
            --shadow: 0 10px 40px rgba(140, 106, 70, 0.12);
            --shadow-soft: 0 15px 35px rgba(0, 0, 0, 0.05);
            --border-light: rgba(140, 106, 70, 0.1);
            --transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            --sidebar-width: 260px;
        }

        /* --- DARK MODE VARIABLES --- */
        body.dark-mode {
            --bg-cream: #161311;
            --bg-header-curve: #221d19;
            --white: #241d18;
            --text-main: #f5ece4;
            --text-muted: #a39c95;
            --shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
            --shadow-soft: 0 15px 35px rgba(0, 0, 0, 0.4);
            --border-light: rgba(212, 163, 115, 0.1);
            --green-dark: #2d6a4f;
            --green-light: #1b4332;
        }

        /* --- RESET & BASE --- */
        * { margin: 0; padding: 0; box-sizing: border-box; outline: none; }

        body {
            background-color: var(--bg-cream);
            font-family: 'Poppins', sans-serif;
            color: var(--text-main);
            overflow-x: hidden;
            line-height: 1.6;
            transition: background-color 0.4s ease, color 0.4s ease;
        }

        .serif { font-family: 'Playfair Display', serif; }

        /* Scroll Animations */
        .reveal { opacity: 0; transform: translateY(30px); transition: var(--transition); }
        .reveal.active { opacity: 1; transform: translateY(0); }

        /* --- SIDEBAR NAVIGATION --- */
        .sidebar {
            width: var(--sidebar-width); height: 100vh; background: var(--white);
            position: fixed; left: 0; top: 0;
            display: flex; flex-direction: column; 
            padding: 2.5rem 1.5rem; border-right: 1px solid var(--border-light);
            z-index: 1000; transition: var(--transition);
        }

        .logo-container {
            margin-bottom: 2.5rem; color: var(--text-main);
            cursor: pointer; transition: var(--transition);
            display: flex; align-items: center; gap: 10px; padding-left: 10px;
        }
        .logo-container h3 { font-weight: 800; font-size: 1.2rem; text-transform: uppercase; letter-spacing: 1px; }

        .menu-label { font-size: 0.75rem; color: var(--text-muted); text-transform: uppercase; letter-spacing: 1px; margin: 1rem 0 0.5rem 10px; font-weight: 600; }

        .nav-links { display: flex; flex-direction: column; gap: 0.5rem; width: 100%; flex: 1; }

        .nav-item {
            color: var(--text-muted); cursor: pointer; transition: var(--transition);
            display: flex; align-items: center; justify-content: flex-start;
            gap: 15px; padding: 0.8rem 1rem; border-radius: 12px; width: 100%; font-weight: 500;
        }

        .nav-item:hover { background: var(--bg-cream); color: var(--bronze-dark); }
        .nav-item.active { background: var(--bg-cream); color: var(--text-main); }
        .nav-item.active i { color: #e76f51; }

        .cart-badge {
            margin-left: auto; background: #d64545; color: white; font-size: 0.65rem;
            font-weight: bold; width: 20px; height: 20px; border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            opacity: 0; transform: scale(0); transition: var(--transition);
        }
        .cart-badge.show { opacity: 1; transform: scale(1); }

        .sidebar-card {
            background: #e0b0ff; border-radius: 16px; padding: 1.5rem; 
            text-align: center; margin-top: auto; position: relative; overflow: hidden;
            border: 1px solid var(--border-light); color: #111;
        }
        .sidebar-card .card-icon { background: transparent; margin: 0 auto 0.5rem; display: flex; justify-content: center; }
        .sidebar-card h4 { font-size: 0.95rem; font-weight: 700; margin-bottom: 2px;}
        .sidebar-card p { font-size: 0.75rem; font-weight: 500;}

        /* --- MAIN WRAPPER & HEADER CURVE --- */
        .main-content { margin-left: var(--sidebar-width); min-height: 100vh; display: flex; flex-direction: column; position: relative; z-index: 1;}
        
        .top-bg-curve {
            position: absolute; top: 0; left: 0; width: 100%; height: 420px;
            background: var(--bg-header-curve);
            border-bottom-left-radius: 80px; z-index: -1; transition: var(--transition);
        }

        .tab-content { display: none; animation: fadeIn 0.4s ease; flex: 1; position: relative; z-index: 1; }
        .tab-content.active { display: block; }
        .tab-container { padding: 4rem 5%; }

        /* --- HEADER & SEARCH --- */
        header { padding: 2rem 5%; display: flex; justify-content: space-between; align-items: flex-start; position: relative; z-index: 900; }
        .header-left { display: flex; flex-direction: column; gap: 1rem; width: 100%; max-width: 600px;}
        .header-left h1 { font-size: 2.5rem; font-weight: 700; color: var(--text-main); margin-bottom: 0.5rem;}
        .header-tools { display: flex; align-items: center; gap: 1.5rem; margin-left: auto; height: fit-content;}
        .theme-toggle { cursor: pointer; color: var(--text-muted); transition: var(--transition); }
        .theme-toggle:hover { color: var(--bronze-dark); transform: rotate(15deg); }

        .user-profile-header {
            display: flex; align-items: center; gap: 12px; cursor: pointer;
            padding: 0.4rem 0.8rem; border-radius: 50px; background: transparent; transition: var(--transition);
        }
        .user-profile-header img { width: 36px; height: 36px; border-radius: 50%; object-fit: cover; }
        .user-profile-header span { font-weight: 500; font-size: 0.9rem; color: var(--text-main); }

        .image-search-bar {
            display: flex; background: var(--white); border-radius: 12px; padding: 0.5rem;
            box-shadow: var(--shadow-soft); width: 100%; align-items: center; border: 1px solid var(--border-light);
        }
        .image-search-bar select {
            border: none; background: transparent; padding: 0.5rem 1rem; color: var(--text-muted);
            border-right: 1px solid var(--border-light); outline: none; cursor: pointer; font-family: inherit; font-weight: 500;
        }
        .image-search-bar input {
            flex: 1; border: none; padding: 0.5rem 1rem; font-family: inherit; background: transparent;
            color: var(--text-main); outline: none;
        }

        /* --- BUTTONS --- */
        .btn-dark {
            background: #112a22; color: white; border: none; border-radius: 8px;
            padding: 0.6rem 1.5rem; cursor: pointer; font-family: inherit; font-weight: 500; transition: var(--transition);
        }
        .btn-dark:hover { transform: translateY(-2px); box-shadow: 0 5px 15px rgba(17, 42, 34, 0.3); }

        .btn-bronze {
            background: var(--bronze-gradient); color: #fff; padding: 1rem 2.5rem; border-radius: 50px; border: none;
            font-weight: 600; font-size: 1rem; cursor: pointer; display: inline-flex; align-items: center; justify-content: center; gap: 10px;
            box-shadow: 0 10px 20px rgba(140, 106, 70, 0.3); transition: var(--transition); font-family: inherit;
        }
        .btn-bronze:hover { transform: translateY(-3px); box-shadow: 0 15px 30px rgba(140, 106, 70, 0.5); }
        .btn-outline { background: transparent; border: 1px solid var(--bronze-light); color: var(--bronze-dark); padding: 0.6rem 1.5rem; border-radius: 8px; cursor: pointer; font-family: inherit; font-weight: 500; transition: var(--transition);}
        .btn-outline:hover { background: var(--bronze-light); color: var(--white); }

        /* --- HOME TAB SPECIFICS --- */
        .section-header { display: flex; justify-content: space-between; align-items: center; margin: 3rem 5% 1.5rem; }
        .section-header h2 { font-size: 1.5rem; font-weight: 600; color: var(--text-main);}
        .view-all-btn { background: var(--white); border: 1px solid var(--border-light); padding: 0.4rem 1rem; border-radius: 50px; font-size: 0.8rem; cursor: pointer; color: var(--text-main);}

        /* Comprehensive Book Grid */
        .collection-grid {
            display: grid; grid-template-columns: repeat(auto-fill, minmax(220px, 1fr)); gap: 2rem; padding: 0 5% 4rem;
        }
        .book-item-card {
            background: var(--white); border-radius: 16px; padding: 1rem; box-shadow: var(--shadow-soft);
            transition: var(--transition); cursor: pointer; display: flex; flex-direction: column; position: relative; border: 1px solid var(--border-light);
        }
        .book-item-card:hover { transform: translateY(-10px); box-shadow: var(--shadow); border-color: var(--bronze-light); }
        .book-img-box { height: 280px; border-radius: 10px; overflow: hidden; margin-bottom: 1rem; position: relative;}
        .book-img-box img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.5s; }
        .book-item-card:hover .book-img-box img { transform: scale(1.05); }
        .book-item-card h4 { font-size: 1.1rem; margin-bottom: 0.2rem; line-height: 1.3;}
        .book-item-card p { font-size: 0.85rem; color: var(--text-muted); margin-bottom: 1rem;}
        .card-bottom { display: flex; justify-content: space-between; align-items: center; margin-top: auto; }
        .card-price { font-weight: 700; color: var(--green-dark); font-size: 1.1rem; }
        .add-icon-btn { background: var(--bg-cream); color: var(--bronze-dark); border: none; width: 35px; height: 35px; border-radius: 50%; display: flex; align-items: center; justify-content: center; cursor: pointer; transition: var(--transition); }
        .add-icon-btn:hover { background: var(--green-dark); color: var(--white); transform: scale(1.1); }

        /* Book Category Categories */
        .cat-wrapper { display: flex; gap: 2rem; overflow-x: auto; padding: 0 5% 5rem; scrollbar-width: none;}
        .cat-item { text-align: center; cursor: pointer; transition: var(--transition); min-width: 120px;}
        .cat-item:hover { transform: translateY(-5px); }
        .cat-img { width: 120px; height: 140px; border-radius: 12px; overflow: hidden; margin-bottom: 0.5rem; box-shadow: var(--shadow-soft); background: var(--white);}
        .cat-img img { width: 100%; height: 100%; object-fit: cover; }
        .cat-item p { font-weight: 600; font-size: 0.9rem; color: var(--text-main);}

        .quote-banner { margin: 2rem 5% 4rem; padding: 4rem 2rem; border-radius: 24px; background: var(--green-dark); color: #fff; text-align: center; box-shadow: 0 20px 40px rgba(27, 67, 50, 0.2); }
        .newsletter { padding: 5rem 5%; text-align: center; background: var(--white); border-top: 1px solid var(--border-light); border-bottom: 1px solid var(--border-light); }
        .newsletter-form { display: flex; max-width: 500px; margin: 0 auto; gap: 10px; }
        .newsletter-form input { flex: 1; padding: 1rem 1.5rem; border-radius: 50px; border: 1px solid var(--border-light); background: var(--bg-cream); color: var(--text-main); font-family: inherit; }

        /* --- BOOK DETAILS MODAL (ABOUT PAGE) --- */
        .book-details-overlay {
            position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0, 0, 0, 0.7); backdrop-filter: blur(8px);
            display: none; justify-content: center; align-items: center; z-index: 3500;
        }
        .book-details-overlay.show { display: flex; animation: fadeIn 0.3s ease; }
        .book-details-card {
            background: var(--white); width: 90%; max-width: 900px; max-height: 90vh;
            border-radius: 24px; box-shadow: 0 25px 50px rgba(0,0,0,0.3);
            display: flex; overflow: hidden; position: relative; border: 1px solid var(--border-light);
        }
        .bd-img { width: 45%; background: var(--bg-cream); position: relative; }
        .bd-img img { width: 100%; height: 100%; object-fit: cover; }
        .bd-content { width: 55%; padding: 3rem; display: flex; flex-direction: column; overflow-y: auto; }
        .bd-close { position: absolute; top: 20px; right: 20px; cursor: pointer; color: var(--text-muted); transition: var(--transition); background: var(--bg-cream); border-radius: 50%; padding: 8px; display: flex; align-items: center; justify-content: center; z-index: 10;}
        .bd-close:hover { color: #d64545; background: #fee2e2;}
        
        .bd-category-badge { display: inline-block; background: var(--green-light); color: var(--green-dark); padding: 0.4rem 1rem; border-radius: 50px; font-size: 0.8rem; font-weight: 600; margin-bottom: 1rem; width: fit-content; text-transform: uppercase; letter-spacing: 1px;}
        .bd-title { font-size: 2.2rem; margin-bottom: 0.5rem; color: var(--text-main); line-height: 1.2; }
        .bd-author { font-size: 1.2rem; color: var(--bronze-dark); margin-bottom: 1.5rem; font-weight: 500; }
        .bd-stats { display: flex; gap: 2rem; margin-bottom: 1.5rem; padding-bottom: 1.5rem; border-bottom: 1px solid var(--border-light); }
        .bd-stat-item { display: flex; flex-direction: column; }
        .bd-stat-lbl { font-size: 0.75rem; color: var(--text-muted); text-transform: uppercase; letter-spacing: 1px; }
        .bd-stat-val { font-size: 1rem; font-weight: 600; color: var(--text-main); }
        .bd-desc { color: var(--text-muted); line-height: 1.8; margin-bottom: 2rem; flex: 1; font-size: 0.95rem; }

        /* --- LIST STRUCTURES (Strict ordered lists) --- */
        .data-list { background: var(--white); border-radius: 16px; padding: 2rem; box-shadow: var(--shadow-soft); border: 1px solid var(--border-light); }
        .data-list ol { list-style-type: decimal; padding-left: 20px; display: flex; flex-direction: column; gap: 1.5rem; }
        .data-list li { padding-bottom: 1.5rem; border-bottom: 1px solid var(--border-light); line-height: 1.8; color: var(--text-main); }
        .data-list li:last-child { border-bottom: none; padding-bottom: 0; }
        .status-overdue { color: #d64545; font-weight: 600; }
        .status-active { color: var(--green-dark); font-weight: 600; }

        /* Form Inputs for Library */
        .add-book-form { display: none; background: var(--bg-cream); padding: 1.5rem; border-radius: 12px; margin-top: 1.5rem; margin-bottom: 2rem; border: 1px solid var(--border-light); animation: fadeIn 0.3s ease; }
        .add-book-form input, .add-book-form select { width: 100%; padding: 0.8rem; margin-bottom: 1rem; border-radius: 8px; border: 1px solid var(--border-light); font-family: inherit; background: var(--white); color: var(--text-main); }

        /* --- ADMIN DASHBOARD TAB --- */
        .admin-stats { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 1.5rem; margin-bottom: 3rem; }
        .stat-card { background: var(--white); padding: 1.5rem; border-radius: 16px; border: 1px solid var(--border-light); box-shadow: var(--shadow-soft); border-left: 4px solid var(--green-dark);}
        .stat-card h4 { color: var(--text-muted); font-size: 0.9rem; font-weight: 500;}
        .stat-card h2 { font-size: 2rem; color: var(--text-main); margin-top: 0.5rem;}

        /* --- AUTH MODAL --- */
        .auth-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0, 0, 0, 0.7); backdrop-filter: blur(8px); display: none; justify-content: center; align-items: center; z-index: 3000; }
        .auth-overlay.show { display: flex; animation: fadeIn 0.3s ease; }
        .auth-card { background: var(--white); width: 90%; max-width: 420px; padding: 3rem; border-radius: 24px; box-shadow: 0 25px 50px rgba(0,0,0,0.3); }
        .auth-card h2 { margin-bottom: 2rem; text-align: center; font-size: 2rem; }
        .form-group { margin-bottom: 1.5rem; }
        .form-group label { display: block; margin-bottom: 0.5rem; font-size: 0.9rem; font-weight: 500; color: var(--text-muted); }
        .form-group input { width: 100%; padding: 0.8rem 1rem; border-radius: 10px; border: 1px solid var(--border-light); background: var(--bg-cream); color: var(--text-main); font-family: inherit; }
        .auth-toggle { text-align: center; margin-top: 1.5rem; font-size: 0.9rem; color: var(--text-muted); cursor: pointer;}
        .auth-toggle span { color: var(--green-dark); font-weight: 600; }

        /* --- CART DRAWER --- */
        .modal-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0, 0, 0, 0.5); backdrop-filter: blur(4px); display: none; z-index: 2000; opacity: 0; transition: opacity 0.3s ease; }
        .modal-overlay.show { opacity: 1; }
        .cart-drawer { position: fixed; top: 0; right: -450px; width: 100%; max-width: 450px; height: 100vh; background: var(--white); box-shadow: -10px 0 40px rgba(0,0,0,0.1); z-index: 2001; transition: right 0.4s cubic-bezier(0.4, 0, 0.2, 1); display: flex; flex-direction: column; border-left: 1px solid var(--border-light); }
        .cart-drawer.open { right: 0; }
        .cart-header { padding: 2rem; border-bottom: 1px solid var(--border-light); display: flex; justify-content: space-between; align-items: center; }
        .cart-body { flex: 1; padding: 2rem; overflow-y: auto; display: flex; flex-direction: column; gap: 1.5rem; }
        .cart-item { display: flex; gap: 1rem; background: var(--bg-cream); padding: 1rem; border-radius: 12px; align-items: center; border: 1px solid var(--border-light);}
        .cart-item img { width: 60px; height: 85px; border-radius: 6px; object-fit: cover; }
        .cart-item-info { flex: 1; }
        .cart-footer { padding: 2rem; border-top: 1px solid var(--border-light); background: var(--bg-cream); }
        .cart-total { display: flex; justify-content: space-between; font-size: 1.2rem; font-weight: 600; margin-bottom: 1.5rem;}

        /* FOOTER, TOAST, COMMUNITY */
        footer { background: var(--white); padding: 6rem 5% 2rem; margin-top: auto; position: relative; z-index: 1;}
        .footer-grid { display: grid; grid-template-columns: 2fr 1fr 1fr; gap: 4rem; }
        .footer-bottom-logo { font-size: clamp(3rem, 15vw, 12rem); color: var(--border-light); text-align: center; margin-top: 4rem; user-select: none; letter-spacing: -2px; }
        
        .forum-post { background: var(--white); padding: 2rem; border-radius: 20px; border: 1px solid var(--border-light); margin-bottom: 1.5rem; transition: var(--transition); }
        .post-header { display: flex; align-items: center; gap: 1rem; margin-bottom: 1rem; }

        .toast-container { position: fixed; bottom: 30px; left: 50%; transform: translateX(-50%); display: flex; flex-direction: column; gap: 10px; z-index: 5000; }
        .toast { background: var(--text-main); color: var(--bg-cream); padding: 1rem 2rem; border-radius: 50px; font-weight: 500; font-size: 0.95rem; display: flex; align-items: center; gap: 10px; box-shadow: 0 10px 30px rgba(0,0,0,0.2); animation: slideUp 0.3s ease forwards, fadeOut 0.3s ease 2.7s forwards; }

        @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
        @keyframes slideUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
        @keyframes fadeOut { from { opacity: 1; } to { opacity: 0; } }

        /* --- MOBILE RESPONSIVE --- */
        @media (max-width: 900px) {
            .sidebar { transform: translateX(-100%); padding: 2rem 1rem; }
            .sidebar.open { transform: translateX(0); box-shadow: var(--shadow); }
            .main-content { margin-left: 0; padding-bottom: 70px; }
            #mobile-menu-btn { display: block !important; margin-right: 15px; cursor: pointer; z-index: 901;}
            header { flex-direction: column; gap: 1.5rem;}
            .header-tools { margin-left: 0; width: 100%; justify-content: space-between;}
            .footer-grid { grid-template-columns: 1fr; text-align: center; }
            .cart-drawer { max-width: 100%; right: -100%; }
            
            .book-details-card { flex-direction: column; max-height: 90vh; overflow-y: auto; }
            .bd-img { width: 100%; height: 250px; }
            .bd-content { width: 100%; padding: 2rem; }
            .bd-close { top: 10px; right: 10px; }
        }
    </style>
</head>
<body>

    <div class="auth-overlay" id="authOverlay">
        <div class="auth-card">
            <h2 id="authTitle" class="serif">Welcome Back</h2>
            <form id="authForm" onsubmit="handleAuth(event)">
                <div class="form-group" id="nameGroup" style="display: none;">
                    <label>Full Name</label>
                    <input type="text" id="authName" placeholder="John Doe">
                </div>
                <div class="form-group">
                    <label>Email Address</label>
                    <input type="email" id="authEmail" placeholder="user@unitylibrary.com" required>
                </div>
                <div class="form-group">
                    <label>Password</label>
                    <input type="password" id="authPassword" placeholder="••••••••" required>
                </div>
                <button type="submit" class="btn-dark" style="width: 100%; padding: 1rem; border-radius: 12px; margin-top: 1rem;" id="authBtn">Log In</button>
                <div class="auth-toggle" onclick="toggleAuthMode()">
                    <span id="authToggleText">Don't have an account? Sign up</span>
                </div>
            </form>
        </div>
    </div>

    <div class="book-details-overlay" id="bookDetailsOverlay">
        <div class="book-details-card">
            <div class="bd-close" onclick="closeBookDetails()"><i data-lucide="x"></i></div>
            <div class="bd-img"><img id="bdImg" src="" alt="Book Cover"></div>
            <div class="bd-content">
                <span class="bd-category-badge" id="bdCategory">Category</span>
                <h2 class="serif bd-title" id="bdTitle">Book Title</h2>
                <p class="bd-author" id="bdAuthor">Author Name</p>
                
                <div class="bd-stats">
                    <div class="bd-stat-item">
                        <span class="bd-stat-lbl">Published</span>
                        <span class="bd-stat-val" id="bdYear">2021</span>
                    </div>
                    <div class="bd-stat-item">
                        <span class="bd-stat-lbl">Pages</span>
                        <span class="bd-stat-val" id="bdPages">300</span>
                    </div>
                    <div class="bd-stat-item">
                        <span class="bd-stat-lbl">Rating</span>
                        <span class="bd-stat-val" style="color: #f59e0b;"><i data-lucide="star" style="width:14px; fill:currentColor;"></i> 4.8</span>
                    </div>
                </div>

                <p class="bd-desc" id="bdDesc">Book description goes here.</p>
                
                <div style="display: flex; justify-content: space-between; align-items: center; margin-top: auto; border-top: 1px solid var(--border-light); padding-top: 1.5rem;">
                    <span style="font-size: 1.8rem; font-weight: 700; color: var(--green-dark);" id="bdPrice">$0.00</span>
                    <button class="btn-dark" id="bdAddBtn" style="padding: 0.8rem 2rem;"><i data-lucide="shopping-bag" style="width:18px; margin-right:8px; vertical-align:middle;"></i> Add to Cart</button>
                </div>
            </div>
        </div>
    </div>

    <nav class="sidebar" id="sidebar">
        <div class="logo-container" onclick="switchTab('tab-home')">
            <h3 class="serif">Unity Library</h3>
        </div>
        
        <div class="nav-links">
            <div class="menu-label">Menu</div>
            <div class="nav-item active" data-target="tab-home"><div style="background:#e76f51; width:24px; height:24px; border-radius:8px; display:flex; justify-content:center; align-items:center;"><i data-lucide="home" style="color:white; width:14px;"></i></div> <span>Discover</span></div>
            <div class="nav-item" data-target="tab-category"><i data-lucide="layout-grid"></i> <span>Category</span></div>
            <div class="nav-item" data-target="tab-library"><i data-lucide="bookmark"></i> <span>My Library</span></div>
            <div class="nav-item" data-target="tab-community"><i data-lucide="users"></i> <span>Community</span></div>
            <div class="nav-item" onclick="openCart()">
                <i data-lucide="heart"></i> <span>Favorite & Cart</span>
                <span class="cart-badge" id="cartBadge">0</span>
            </div>
            
            <div class="menu-label" style="margin-top: 1.5rem;">Settings & Admin</div>
            <div class="nav-item" data-target="tab-admin" id="nav-admin" style="display:none;"><i data-lucide="shield"></i> <span>Admin Panel</span></div>
            <div class="nav-item" data-target="tab-settings"><i data-lucide="settings"></i> <span>Setting</span></div>
            <div class="nav-item" onclick="logout()"><i data-lucide="log-out"></i> <span>Log out</span></div>
        </div>

        <div class="sidebar-card">
            <div class="card-icon">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/><path d="M2 12h20"/></svg>
            </div>
            <h4 class="serif">BOOK LIBRARY</h4>
            <p>E-Corner Kiosk</p>
        </div>
    </nav>

    <div class="main-content">
        <div class="top-bg-curve"></div>
        
        <header id="main-header">
            <div class="header-left">
                <div style="display:flex; align-items:center;">
                    <i data-lucide="menu" id="mobile-menu-btn" style="display: none;" onclick="document.getElementById('sidebar').classList.toggle('open')"></i>
                    <h1 id="dynamic-title" class="serif">Discover</h1>
                </div>
                <div class="image-search-bar">
                    <select>
                        <option>All Categories</option>
                        <option>Business</option>
                        <option>Design</option>
                        <option>Fiction</option>
                    </select>
                    <i data-lucide="search" style="margin-left: 10px; color: var(--text-muted); width: 18px;"></i>
                    <input type="text" placeholder="find the book you like....">
                    <button class="btn-dark">Search</button>
                </div>
            </div>

            <div class="header-tools">
                <div class="theme-toggle" onclick="toggleTheme()" title="Toggle Dark Mode">
                    <i data-lucide="moon" id="themeIcon"></i>
                </div>
                <div class="user-profile-header" onclick="switchTab('tab-settings')" title="Account Settings">
                    <img src="https://ui-avatars.com/api/?name=User&background=1b4332&color=fff" id="header-avatar" alt="Profile">
                    <span id="header-username">Guest</span>
                    <i data-lucide="chevron-down" style="width: 14px; color: var(--text-muted);"></i>
                </div>
                <div class="nav-item" style="width:auto; height:auto; color: var(--text-muted); cursor:pointer;"><i data-lucide="bell"></i></div>
            </div>
        </header>

        <div id="tab-home" class="tab-content active">
            
            <div class="section-header reveal">
                <h2 class="serif">Browse Collection</h2>
                <button class="view-all-btn">View all ></button>
            </div>

            <section class="collection-grid reveal" id="collection-grid">
                </section>

            <div class="section-header reveal" style="margin-top: 1rem;">
                <h2 class="serif">Browse by Category</h2>
                <button class="view-all-btn"><i data-lucide="sliders-horizontal" style="width:14px; vertical-align:middle;"></i></button>
            </div>

            <section class="cat-wrapper reveal">
                <div class="cat-item">
                    <div class="cat-img"><img src="https://images.unsplash.com/photo-1592496431122-2349e0fbc666?auto=format&fit=crop&q=80&w=200" alt="Finance"></div>
                    <p>Finance</p>
                </div>
                <div class="cat-item">
                    <div class="cat-img"><img src="https://images.unsplash.com/photo-1561070791-2526d30994b5?auto=format&fit=crop&q=80&w=200" alt="Design"></div>
                    <p>Design</p>
                </div>
                <div class="cat-item">
                    <div class="cat-img"><img src="https://images.unsplash.com/photo-1454165804606-c3d57bc86b40?auto=format&fit=crop&q=80&w=200" alt="Business"></div>
                    <p>Business</p>
                </div>
                <div class="cat-item">
                    <div class="cat-img"><img src="https://images.unsplash.com/photo-1589829085413-56de8ae18c73?auto=format&fit=crop&q=80&w=200" alt="Self Help"></div>
                    <p>Self Help</p>
                </div>
                <div class="cat-item">
                    <div class="cat-img"><img src="https://images.unsplash.com/photo-1544947950-fa07a98d237f?auto=format&fit=crop&q=80&w=200" alt="Fiction"></div>
                    <p>Fiction</p>
                </div>
            </section>

            <section class="quote-banner reveal">
                <h1 class="serif" style="color: white; margin-bottom: 1rem;">"A reader lives a thousand lives before he dies. The man who never reads lives only one."</h1>
                <p style="color: white; font-weight: 500; text-transform: uppercase;">— George R.R. Martin</p>
            </section>

            <section class="newsletter reveal">
                <h2 class="serif">Join our Newsletter</h2>
                <p style="color: var(--text-muted); margin-bottom: 2rem;">Get the latest releases directly to your inbox.</p>
                <div class="newsletter-form">
                    <input type="email" placeholder="Your email address" id="newsletter-input">
                    <button class="btn-bronze" onclick="subscribeNewsletter()">Subscribe</button>
                </div>
            </section>
        </div>

        <div id="tab-library" class="tab-content">
            <div class="tab-container">
                <div class="data-list reveal">
                    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem; flex-wrap: wrap; gap: 1rem;">
                        <h2 class="serif">My Borrowed Books</h2>
                        <button class="btn-dark" onclick="toggleAddBookForm()">+ Add Previously Borrowed</button>
                    </div>
                    
                    <div class="add-book-form" id="addBookForm">
                        <h4 class="serif" style="margin-bottom: 1rem;">Add a Previous Book</h4>
                        <input type="text" id="newBookTitle" placeholder="Enter Book Title">
                        <div style="display: flex; gap: 1rem;">
                            <input type="date" id="newBookDate" style="flex:1;">
                            <select id="newBookStatus" style="flex:1; margin-bottom: 1rem;">
                                <option value="Returned">Returned</option>
                                <option value="Active">Active</option>
                            </select>
                        </div>
                        <div style="display: flex; gap: 1rem; margin-top: 1rem;">
                            <button class="btn-bronze" onclick="submitNewBook()">Save Record</button>
                            <button class="btn-outline" onclick="toggleAddBookForm()">Cancel</button>
                        </div>
                    </div>

                    <ol id="library-list">
                        </ol>
                </div>
            </div>
        </div>
        
        <div id="tab-category" class="tab-content"><div class="tab-container"><h2 class="serif">Categories</h2><p>Explore all categories in detail here.</p></div></div>

        <div id="tab-community" class="tab-content">
            <div class="tab-container">
                <div class="forum-post reveal">
                    <div class="post-header">
                        <img src="https://ui-avatars.com/api/?name=Marcus&background=bfa58a&color=fff" style="width:40px; border-radius:50%;" alt="User">
                        <div>
                            <h4>Marcus Thorne</h4>
                            <p style="font-size: 0.8rem; color: var(--text-muted);">2 hours ago</p>
                        </div>
                    </div>
                    <p>Just finished reading <strong>The Psychology of Money</strong>. The lessons on compounding are completely unexpected! Has anyone else read the final chapter yet?</p>
                    <div style="margin-top: 1rem; color: var(--bronze-dark); display: flex; gap: 1rem; cursor: pointer;">
                        <span><i data-lucide="heart" style="width:16px; vertical-align:middle;"></i> 24</span>
                        <span><i data-lucide="message-square" style="width:16px; vertical-align:middle;"></i> 5 Replies</span>
                    </div>
                </div>
                
                <div class="forum-post reveal">
                    <div class="post-header">
                        <img src="https://ui-avatars.com/api/?name=Elena&background=1b4332&color=fff" style="width:40px; border-radius:50%;" alt="User">
                        <div>
                            <h4>Elena Rostova</h4>
                            <p style="font-size: 0.8rem; color: var(--text-muted);">5 hours ago</p>
                        </div>
                    </div>
                    <p>Looking for recommendations on clean architecture and design patterns. I loved Robert C. Martin's work. What should I pick up next?</p>
                    <div style="margin-top: 1rem; color: var(--bronze-dark); display: flex; gap: 1rem; cursor: pointer;">
                        <span><i data-lucide="heart" style="width:16px; vertical-align:middle;"></i> 12</span>
                        <span><i data-lucide="message-square" style="width:16px; vertical-align:middle;"></i> 18 Replies</span>
                    </div>
                </div>
            </div>
        </div>

        <div id="tab-admin" class="tab-content">
            <div class="tab-container" style="padding-top: 2rem;">
                <div style="display:flex; justify-content: space-between; margin-bottom: 2rem; align-items:center;">
                    <h2 class="serif">Live Database Overview</h2>
                    <button class="btn-dark" onclick="syncSheets()"><i data-lucide="refresh-cw" style="width:16px; margin-right:5px; vertical-align:middle;"></i> Sync Google Sheets</button>
                </div>
                
                <div class="admin-stats reveal">
                    <div class="stat-card"><h4>Total Inventory</h4><h2>1,248</h2></div>
                    <div class="stat-card"><h4>Active Borrows</h4><h2>342</h2></div>
                    <div class="stat-card"><h4>Overdue</h4><h2 style="color: #d64545;">18</h2></div>
                    <div class="stat-card"><h4>Registered Users</h4><h2>892</h2></div>
                </div>

                <div class="data-list reveal">
                    <h3 class="serif" style="margin-bottom: 1.5rem;">Recent Borrowing History</h3>
                    <ol id="admin-list-body">
                        </ol>
                </div>
            </div>
        </div>

        <div id="tab-settings" class="tab-content">
            <div class="tab-container">
                <div class="settings-card reveal" style="background: var(--white); padding: 2.5rem; border-radius: 20px; border: 1px solid var(--border-light); max-width: 600px;">
                    <h2 class="serif" style="margin-bottom: 1.5rem;">Profile Settings</h2>
                    <div class="form-group" style="margin-bottom: 1.5rem;">
                        <label style="display:block; margin-bottom:0.5rem; font-weight:500;">Display Name</label>
                        <input type="text" id="settingsName" placeholder="Enter your name" style="width:100%; padding:1rem; border-radius:10px; border:1px solid var(--border-light); background:var(--bg-cream); font-family:inherit; color: var(--text-main);">
                    </div>
                    <button class="btn-bronze" onclick="saveSettings()">Save Changes</button>
                </div>
            </div>
        </div>

        <footer>
            <div class="footer-grid">
                <div>
                    <h3 class="serif" style="font-size: 1.8rem; margin-bottom: 1rem;">Let's connect <i data-lucide="external-link"></i></h3>
                    <p style="color: var(--text-muted);">Unity Library E-Corner. Bringing physical reading to the digital era.</p>
                </div>
                <div>
                    <h4 style="margin-bottom: 1rem; color: var(--text-muted); text-transform: uppercase; font-size: 0.85rem;">Website</h4>
                    <p style="cursor: pointer; margin-bottom: 0.5rem;" onclick="switchTab('tab-home')">Home</p>
                    <p style="cursor: pointer; margin-bottom: 0.5rem;" onclick="switchTab('tab-library')">Library</p>
                </div>
                <div>
                    <h4 style="margin-bottom: 1rem; color: var(--text-muted); text-transform: uppercase; font-size: 0.85rem;">Legal</h4>
                    <p style="cursor: pointer; margin-bottom: 0.5rem;">Privacy Policy</p>
                    <p style="margin-top: 2rem; font-size: 0.9rem; color: var(--text-muted);">© 2026 Unity Library</p>
                </div>
            </div>
            <div class="footer-bottom-logo serif">Unity Library</div>
        </footer>
    </div>

    <div class="modal-overlay" id="cartOverlay" onclick="closeCart()"></div>
    <div class="cart-drawer" id="cartDrawer">
        <div class="cart-header">
            <h3 class="serif">Your Cart & Favorites</h3>
            <div onclick="closeCart()" style="cursor:pointer; color:var(--text-muted); transition: var(--transition);"><i data-lucide="x"></i></div>
        </div>
        <div class="cart-body" id="cartBody">
            <p style="text-align:center; color:var(--text-muted); margin-top:2rem;">Your cart is currently empty.</p>
        </div>
        <div class="cart-footer">
            <div class="cart-total"><span>Total:</span><span id="cartTotal">$0.00</span></div>
            <button class="btn-dark" style="width: 100%; justify-content: center; padding: 1rem;" onclick="showToast('Proceeding to checkout...', 'credit-card')">Checkout</button>
        </div>
    </div>

    <div class="toast-container" id="toastBox"></div>

    <script>
        // 1. Icons Init
        lucide.createIcons();

        // 2. Auth & Session Simulation
        let currentUser = null;
        let isSignupMode = false;

        window.onload = () => {
            if (!localStorage.getItem('unity_lib_session_v4')) {
                const overlay = document.getElementById('authOverlay');
                overlay.style.display = 'flex';
                setTimeout(() => overlay.classList.add('show'), 10);
            } else {
                currentUser = JSON.parse(localStorage.getItem('unity_lib_user_v4'));
                updateAuthUI();
            }
            renderStoreData();
            renderUserLibrary();
            renderAdminData();
        };

        function toggleAuthMode() {
            isSignupMode = !isSignupMode;
            document.getElementById('authTitle').innerText = isSignupMode ? 'Create Account' : 'Welcome Back';
            document.getElementById('authBtn').innerText = isSignupMode ? 'Sign Up' : 'Log In';
            document.getElementById('authToggleText').innerText = isSignupMode ? 'Already have an account? Log in' : "Don't have an account? Sign up";
            document.getElementById('nameGroup').style.display = isSignupMode ? 'block' : 'none';
        }

        function handleAuth(e) {
            e.preventDefault();
            const email = document.getElementById('authEmail').value;
            const name = isSignupMode ? document.getElementById('authName').value : email.split('@')[0];
            
            currentUser = { name: name || 'User', email: email, role: email.includes('admin') ? 'admin' : 'customer' };
            localStorage.setItem('unity_lib_session_v4', 'true');
            localStorage.setItem('unity_lib_user_v4', JSON.stringify(currentUser));
            
            document.getElementById('authOverlay').classList.remove('show');
            setTimeout(() => document.getElementById('authOverlay').style.display = 'none', 300);
            
            showToast(`Connected securely. Welcome, ${currentUser.name}!`, 'shield-check');
            updateAuthUI();
        }

        function logout() {
            localStorage.removeItem('unity_lib_session_v4');
            localStorage.removeItem('unity_lib_user_v4');
            location.reload();
        }

        function updateAuthUI() {
            if(currentUser) {
                document.getElementById('header-username').innerText = currentUser.name;
                document.getElementById('header-avatar').src = `https://ui-avatars.com/api/?name=${currentUser.name}&background=1b4332&color=fff`;
                document.getElementById('settingsName').value = currentUser.name;
                
                if(currentUser.role === 'admin') {
                    document.getElementById('nav-admin').style.display = 'flex';
                }
            }
        }

        function saveSettings() {
            if(currentUser) {
                currentUser.name = document.getElementById('settingsName').value;
                localStorage.setItem('unity_lib_user_v4', JSON.stringify(currentUser));
                updateAuthUI();
                showToast('Settings saved to database', 'database');
            }
        }

        // 3. Tab Logic
        const navItems = document.querySelectorAll('.sidebar .nav-item[data-target]');
        const tabContents = document.querySelectorAll('.tab-content');
        const dynamicTitle = document.getElementById('dynamic-title');

        function switchTab(targetId) {
            navItems.forEach(item => {
                if(item.getAttribute('data-target') === targetId) {
                    item.classList.add('active');
                    const spanText = item.querySelector('span').innerText;
                    dynamicTitle.innerText = spanText;
                } else {
                    item.classList.remove('active');
                }
            });

            tabContents.forEach(tab => {
                tab.classList.remove('active');
                if (tab.id === targetId) {
                    tab.classList.add('active');
                    window.scrollTo({ top: 0, behavior: 'smooth' });
                }
            });
            document.getElementById('sidebar').classList.remove('open');
        }

        navItems.forEach(item => {
            item.addEventListener('click', () => switchTab(item.getAttribute('data-target')));
        });

        // 4. EXTANDED Data Injection & About Modal
        const storeBooks = [
            { id: 1, title: "The Psychology of Money", author: "Morgan Housel", category: "Finance", pages: 342, year: 2020, price: 24.99, img: "https://images.unsplash.com/photo-1592496431122-2349e0fbc666?auto=format&fit=crop&q=80&w=400", desc: "Timeless lessons on wealth, greed, and happiness. Doing well with money isn't necessarily about what you know. It's about how you behave. And behavior is hard to teach, even to really smart people." },
            { id: 2, title: "Company of One", author: "Paul Jarvis", category: "Business", pages: 272, year: 2019, price: 18.50, img: "https://images.unsplash.com/photo-1554774853-719586f82d77?auto=format&fit=crop&q=80&w=400", desc: "What if the real key to a richer and more fulfilling career was not to create and scale a new startup, but rather, to be able to work for yourself, determine your own hours, and become a highly profitable and sustainable company of one?" },
            { id: 3, title: "How Innovation Works", author: "Matt Ridley", category: "History/Tech", pages: 416, year: 2020, price: 21.00, img: "https://images.unsplash.com/photo-1589829085413-56de8ae18c73?auto=format&fit=crop&q=80&w=400", desc: "Building on his bestseller The Rational Optimist, Matt Ridley chronicles the history of innovation, arguing that we need to change the way we think about it, from a top-down process to an organic, bottom-up one." },
            { id: 4, title: "The Picture of Dorian Gray", author: "Oscar Wilde", category: "Fiction", pages: 254, year: 1890, price: 15.99, img: "https://images.unsplash.com/photo-1544947950-fa07a98d237f?auto=format&fit=crop&q=80&w=400", desc: "Oscar Wilde’s only novel is the dreamlike story of a young man who sells his soul for eternal youth and beauty. A classic that remains as relevant today as when it was first published." },
            { id: 5, title: "Atomic Habits", author: "James Clear", category: "Self-Help", pages: 320, year: 2018, price: 19.99, img: "https://images.unsplash.com/photo-1512820790803-83ca734da794?auto=format&fit=crop&q=80&w=400", desc: "No matter your goals, Atomic Habits offers a proven framework for improving--every day. James Clear reveals practical strategies to form good habits and break bad ones." },
            { id: 6, title: "Deep Work", author: "Cal Newport", category: "Self-Help", pages: 304, year: 2016, price: 22.50, img: "https://images.unsplash.com/photo-1629992101753-56d196c8aced?auto=format&fit=crop&q=80&w=400", desc: "Rules for focused success in a distracted world. Deep work is the ability to focus without distraction on a cognitively demanding task. It's a skill that allows you to quickly master complicated information." },
            { id: 7, title: "Thinking, Fast and Slow", author: "Daniel Kahneman", category: "Psychology", pages: 499, year: 2011, price: 25.00, img: "https://images.unsplash.com/photo-1532012197267-da84d127e765?auto=format&fit=crop&q=80&w=400", desc: "The phenomenal New York Times bestseller by the Nobel Prize-winning psychologist. Kahneman takes us on a groundbreaking tour of the mind and explains the two systems that drive the way we think." },
            { id: 8, title: "Sapiens", author: "Yuval Noah Harari", category: "History", pages: 464, year: 2015, price: 20.00, img: "https://images.unsplash.com/photo-1589998059171-988d887df646?auto=format&fit=crop&q=80&w=400", desc: "A brief history of humankind. Earth is 4.5 billion years old. In just a fraction of that time, one species among countless others has conquered it: us. Harari explores how we got here." },
            { id: 9, title: "1984", author: "George Orwell", category: "Fiction", pages: 328, year: 1949, price: 14.50, img: "https://images.unsplash.com/photo-1519681393784-d120267933ba?auto=format&fit=crop&q=80&w=400", desc: "Among the seminal texts of the 20th century, Nineteen Eighty-Four is a rare work that grows more haunting as its futuristic purgatory becomes more real." },
            { id: 10, title: "Clean Code", author: "Robert C. Martin", category: "Design/Tech", pages: 464, year: 2008, price: 45.00, img: "https://images.unsplash.com/photo-1555066931-4365d14bab8c?auto=format&fit=crop&q=80&w=400", desc: "Even bad code can function. But if code isn't clean, it can bring a development organization to its knees. A handbook of agile software craftsmanship." },
            { id: 11, title: "The Alchemist", author: "Paulo Coelho", category: "Fiction", pages: 163, year: 1988, price: 16.99, img: "https://images.unsplash.com/photo-1543002588-bfa74002ed7e?auto=format&fit=crop&q=80&w=400", desc: "Paulo Coelho's enchanting novel has inspired a devoted following around the world. This story, dazzling in its powerful simplicity and inspiring wisdom, is about an Andalusian shepherd boy." },
            { id: 12, title: "Steve Jobs", author: "Walter Isaacson", category: "Biography", pages: 656, year: 2011, price: 28.00, img: "https://images.unsplash.com/photo-1505664177941-ebf4fa0e8b1b?auto=format&fit=crop&q=80&w=400", desc: "Based on more than forty interviews with Jobs conducted over two years—as well as interviews with more than a hundred family members, friends, adversaries, competitors, and colleagues." }
        ];

        function renderStoreData() {
            const recGrid = document.getElementById('collection-grid');
            recGrid.innerHTML = '';
            storeBooks.forEach(book => {
                recGrid.innerHTML += `
                    <div class="book-item-card" onclick="openBookDetails(${book.id})">
                        <div class="book-img-box">
                            <img src="${book.img}" alt="${book.title}">
                        </div>
                        <h4 class="serif">${book.title}</h4>
                        <p>${book.author}</p>
                        <div class="card-bottom">
                            <span class="card-price">$${book.price.toFixed(2)}</span>
                            <button class="add-icon-btn" onclick="event.stopPropagation(); addToCart(${book.id})" title="Add to Cart">
                                <i data-lucide="plus" style="width:20px;"></i>
                            </button>
                        </div>
                    </div>`;
            });
            lucide.createIcons();
            window.dispatchEvent(new Event('scroll'));
        }

        // Book Details Modal Logic
        function openBookDetails(id) {
            const book = storeBooks.find(b => b.id === id);
            if(!book) return;
            
            document.getElementById('bdImg').src = book.img;
            document.getElementById('bdTitle').innerText = book.title;
            document.getElementById('bdAuthor').innerText = book.author;
            document.getElementById('bdCategory').innerText = book.category;
            document.getElementById('bdYear').innerText = book.year;
            document.getElementById('bdPages').innerText = book.pages;
            document.getElementById('bdDesc').innerText = book.desc;
            document.getElementById('bdPrice').innerText = `$${book.price.toFixed(2)}`;
            
            const addBtn = document.getElementById('bdAddBtn');
            addBtn.onclick = () => {
                addToCart(book.id);
                closeBookDetails();
            };

            const overlay = document.getElementById('bookDetailsOverlay');
            overlay.style.display = 'flex';
        }

        function closeBookDetails() {
            document.getElementById('bookDetailsOverlay').style.display = 'none';
        }

        // Feature: Custom Borrowed Books Tracker
        let myLibraryBooks = [
            { title: "Deep Work", date: "2026-04-20", status: "Active" },
            { title: "Atomic Habits", date: "2025-11-15", status: "Returned" }
        ];

        function renderUserLibrary() {
            const list = document.getElementById('library-list');
            list.innerHTML = '';
            myLibraryBooks.forEach((book, index) => {
                const statusColor = book.status === "Active" ? "color: var(--green-dark)" : "color: var(--text-muted)";
                list.innerHTML += `
                    <li>
                        <strong>${book.title}</strong><br>
                        Borrowed: ${book.date} | Status: <span style="font-weight:600; ${statusColor}">${book.status}</span><br>
                        <button class="btn-outline" style="margin-top:10px; padding: 0.4rem 1rem;" onclick="removeLibraryBook(${index})">Remove Record</button>
                    </li>
                `;
            });
        }

        function toggleAddBookForm() {
            const form = document.getElementById('addBookForm');
            form.style.display = form.style.display === 'none' || form.style.display === '' ? 'block' : 'none';
        }

        function submitNewBook() {
            const title = document.getElementById('newBookTitle').value;
            const date = document.getElementById('newBookDate').value;
            const status = document.getElementById('newBookStatus').value;

            if(title && date) {
                myLibraryBooks.push({ title, date, status });
                renderUserLibrary();
                document.getElementById('newBookTitle').value = '';
                document.getElementById('newBookDate').value = '';
                toggleAddBookForm();
                showToast('Record saved to your library!', 'check-circle');
            } else {
                showToast('Please fill all fields', 'alert-circle');
            }
        }

        function removeLibraryBook(index) {
            myLibraryBooks.splice(index, 1);
            renderUserLibrary();
            showToast('Record removed', 'trash-2');
        }

        // Admin Data
        const adminBorrows = [
            { user: "Davis Workman", title: "The Psychology of Money", due: "2026-04-24", status: "Overdue" },
            { user: "Elena Rostova", title: "Company of One", due: "2026-04-28", status: "Active" },
            { user: "Marcus Thorne", title: "How Innovation Works", due: "2026-05-02", status: "Active" }
        ];

        function renderAdminData() {
            const adminBody = document.getElementById('admin-list-body');
            adminBody.innerHTML = '';
            adminBorrows.forEach(item => {
                const statusClass = item.status === 'Overdue' ? 'status-overdue' : 'status-active';
                adminBody.innerHTML += `
                    <li>
                        <strong>${item.user}</strong> borrowed <em>${item.title}</em> (Due: ${item.due}) - <span class="${statusClass}">${item.status}</span>
                    </li>`;
            });
        }

        // 5. Cart Logic
        let cart = [];
        const cartDrawer = document.getElementById('cartDrawer');
        const cartOverlay = document.getElementById('cartOverlay');
        const cartBody = document.getElementById('cartBody');
        const cartTotal = document.getElementById('cartTotal');
        const cartBadge = document.getElementById('cartBadge');

        function openCart() {
            cartDrawer.classList.add('open');
            cartOverlay.style.display = 'block';
            setTimeout(() => cartOverlay.classList.add('show'), 10);
            updateCartUI();
        }

        function closeCart() {
            cartDrawer.classList.remove('open');
            cartOverlay.classList.remove('show');
            setTimeout(() => cartOverlay.style.display = 'none', 300);
        }

        function addToCart(id) {
            const book = storeBooks.find(b => b.id === id);
            cart.push(book);
            showToast(`${book.title} added!`, 'check-circle');
            cartBadge.innerText = cart.length;
            cartBadge.classList.add('show');
            updateCartUI();
        }

        function removeFromCart(index) {
            cart.splice(index, 1);
            cartBadge.innerText = cart.length;
            if(cart.length === 0) cartBadge.classList.remove('show');
            updateCartUI();
        }

        function updateCartUI() {
            if(cart.length === 0) {
                cartBody.innerHTML = '<p style="text-align:center; color:var(--text-muted); margin-top:2rem;">Your list is currently empty.</p>';
                cartTotal.innerText = '$0.00';
                return;
            }

            cartBody.innerHTML = '';
            let total = 0;
            cart.forEach((item, index) => {
                total += item.price;
                cartBody.innerHTML += `
                    <div class="cart-item">
                        <img src="${item.img}" alt="${item.title}">
                        <div class="cart-item-info">
                            <h5 class="serif" style="margin-bottom:5px; color: var(--text-main);">${item.title}</h5>
                            <p style="color:var(--bronze-dark); font-weight:600;">$${item.price.toFixed(2)}</p>
                        </div>
                        <i data-lucide="trash-2" onclick="removeFromCart(${index})" style="color:#d64545; cursor:pointer; padding:5px;"></i>
                    </div>
                `;
            });
            cartTotal.innerText = `$${total.toFixed(2)}`;
            lucide.createIcons();
        }

        // 6. Extras
        function syncSheets() {
            showToast('Syncing with Google Sheets API...', 'cloud-lightning');
            setTimeout(() => showToast('Live Database synced successfully!', 'check-circle'), 1500);
        }

        function subscribeNewsletter() {
            const input = document.getElementById('newsletter-input');
            if(input.value.includes('@')) {
                showToast('Subscribed successfully!', 'mail');
                input.value = '';
            } else showToast('Please enter a valid email.', 'alert-circle');
        }

        let isDarkMode = false;
        function toggleTheme() {
            isDarkMode = !isDarkMode;
            document.body.classList.toggle('dark-mode');
            document.getElementById('themeIcon').setAttribute('data-lucide', isDarkMode ? 'sun' : 'moon');
            showToast(isDarkMode ? 'Dark mode enabled' : 'Light mode enabled', isDarkMode ? 'moon' : 'sun');
            lucide.createIcons();
        }

        function showToast(msg, iconName = 'info') {
            const toast = document.createElement('div');
            toast.classList.add('toast');
            toast.innerHTML = `<i data-lucide="${iconName}"></i> ${msg}`;
            document.getElementById('toastBox').appendChild(toast);
            lucide.createIcons();
            setTimeout(() => toast.remove(), 3000);
        }

        window.addEventListener('scroll', () => {
            document.querySelectorAll('.reveal').forEach(el => {
                if (el.getBoundingClientRect().top < window.innerHeight - 50) el.classList.add('active');
            });
        });
    </script>
</body>
</html>
